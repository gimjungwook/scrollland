# ScrollLand에 기여하기

ScrollLand는 스크롤하며 본문과 예제를 읽고, 필요한 구간에서 실행 과정이나 관계의 변화를 살펴보는 한국어 Python 학습 자료입니다. 학습자가 해결할 문제와 목표를 먼저 확인하고, 그 문제를 이해하는 데 필요한 설명과 예제를 작성합니다.

## 원본과 생성물

`curriculum.json`은 코스·레슨 순서, `lessons/`는 레슨 원본, `motion.json`은 채택한 시각화의 상태 자료입니다. 생성된 `index.html`과 `learn/`의 HTML은 직접 수정하지 않습니다. 자료의 출처를 적고 다른 교재의 문장이나 예제를 복사하지 않습니다.

새 개념은 사용하는 자리에서 뜻을 풀이하고 필요한 선행 레슨을 연결합니다. 레슨을 참조할 때는 독자가 되돌아가 찾지 않아도 대상을 알도록 제목을 함께 씁니다. 예제 코드·출력·도식·상태 설명은 같은 상황과 시점을 설명해야 합니다. 교수 검토나 학생 체험이 확인되지 않은 내용을 승인·효과 검증 완료로 쓰지 않습니다.

## 코스와 레슨 도입

전체 문서는 코스 도입 뒤에 해당 코스의 레슨들을 배치합니다. 첫 코스의 도입은 서비스 소개도 겸합니다. 개별 레슨 주소에는 전체 문서와 같은 레슨 도입을 넣고, 코스의 큰 도입을 매번 중복하지 않습니다.

| 자료 | 필수 항목 | 작성 기준 |
| --- | --- | --- |
| `course.intro` | `headline`, `premise`, `payoff`, `scene` | `headline`은 비어 있지 않은 제목 줄의 배열이며 각 줄도 비어 있지 않아야 함. 줄 수는 의미에 맞춰 정하며 이번 8개 코스 원고는 두 줄을 사용. `premise`는 다룰 상황, `payoff`는 학습 뒤 확인할 일. `scene`은 `before`·`after`·`caption` 문자열 |
| `lesson.intro` | `hook`, `connection`, `payoff`, `preview` | `hook`은 구체적인 상황이나 질문, `connection`은 앞선 학습과 이번 필요의 연결, `payoff`는 관찰할 결과. `preview`는 `before`·`after` 문자열 |

분류용 `course.title`과 기존 레슨의 `problem`·`goal`을 그대로 복사해 도입 분량만 늘리지 않습니다. 레슨 화면에서는 `intro.hook`을 제목 아래 전체 폭으로 표시하고, `intro.connection`·`intro.payoff`·`lesson.goal`을 이어 읽습니다. `lesson.problem`은 교육 원본에 보존하되 도입 화면에 같은 질문을 중복 표시하지 않습니다. 미리보기는 해당 예제의 코드·값·결과 또는 설명임을 구별한 요약에서 고르고, 호출과 실행·반환과 출력·같은 객체와 복사본을 혼동시키지 않습니다. 예고한 결과를 기존 예제와 대조하고 [도입 콘텐츠 기록](docs/intro-content.md)에 선택 근거를 남깁니다.

`scripts/render-intro.mjs`는 같은 형식 안에서 코스별 상징 그림 8종을 재사용합니다. 그림과 제목의 배치·크기·선이 스크롤 위치에 따라 변하며, 레슨 도입에는 해당 코스의 그림을 작게 사용합니다. 코스 제목의 모든 줄과 도입 설명은 연출 전에도 읽을 수 있어야 합니다. 상징 그림은 코스의 주제를 나타내며 실행 상태를 재현하는 시각화와 구별합니다.

`lib/intro-motion.js`의 코스 도입은 화면 안에 도입 전체가 들어가면 전체를 고정합니다. 전체를 담을 수 없으면 그림·캡션·전후 미리보기 영역만 고정하며, 그 영역도 담을 수 없으면 완성된 정적 그림을 표시합니다. 고정 위치는 머리말 아래 12px이며, 관찰용 추가 스크롤 거리는 화면 높이의 70%를 320~720px 범위로 제한한 값입니다. 마지막 15% 거리에서는 완성된 모양을 유지합니다. 긴 설명을 고정 영역에 억지로 넣거나 화면 밖으로 잘라서는 안 됩니다.

작은 레슨 도입은 제목과 그림을 담은 영역의 위쪽이 화면 높이의 85%에서 45%로 이동하는 동안 연출합니다. 제목이 화면 밖으로 지나가기 전에 연출을 마치며, 고정이나 관찰용 추가 거리를 두지 않습니다. 코스·레슨 모두 같은 스크롤 위치에서 같은 배치를 복원하고, 시간 재생 완료 대기·제목이나 본문 숨김을 넣지 않습니다. JavaScript 미사용·움직임 줄이기·실패 시에는 완성된 정적 그림과 같은 글을 남깁니다.

도입 자료는 `motion.json`의 실행 상태나 본문 문구의 `effect`에 넣지 않습니다. 도입 제목·상징 그림, 본문 문구 효과, 학습 상태 시각화는 서로 다른 목적과 실행 경로를 사용합니다.

## 읽기 구간과 시각화 선택

레슨에는 `sections`를 두고 각 구간에 고유한 `id`, `kind`, `title`, `body`를 지정합니다. `id`는 전체 교육 원본에서 겹치지 않는 영문 소문자·숫자·하이픈 식별자입니다. 기존 예제의 식별자는 앞에 글 구간을 추가해도 바꾸지 않습니다.

| 구간 | 작성 기준 | 추가 자료 |
| --- | --- | --- |
| `reading` | 본문을 읽고 정적 자료를 비교해 이해할 수 있는 내용 | `code`·`output`, `diagram`, `trace`를 필요에 따라 추가 |
| `visualization` | 실행 경로나 유지·변경되는 관계를 같은 자리에서 추적해야 하는 내용 | `question`, `rationale`, `interpretation`, `visualization`, 도식·상태 자료 필수. 코드 예제는 필요할 때만 추가 |

글만 있는 구간도 완전한 학습 구간입니다. 모든 구간에 예제·도식·상태 설명을 붙이거나, 레슨마다 같은 구간 수·단락 수·시각화 수를 채우지 않습니다. `reading`에 `code`를 넣으면 `output`도 지정합니다. 출력이 없다면 빈 문자열을 사용합니다.

시각화의 `question`은 관찰할 질문, `rationale`은 움직임이 필요한 제작 근거, `interpretation`은 관찰 후 읽을 해석입니다. 본문과 질문은 무대 앞에, 해석은 무대 뒤에 계속 남습니다. 스크롤 진행률에 따라 본문을 숨기거나 다른 단락으로 교체하지 않습니다.

`visualization`은 아래 항목을 갖습니다.

- `ref`: 해당 구간 `id`와 같은 값이며 `motion.json`의 `scenes` 키와 연결합니다.
- `pin`: 관찰하는 동안 무대를 화면에 유지할지 명시합니다. 현재 선택한 14개 시각화는 모두 `true`입니다. 현재 화면에 무대 전체가 들어가지 않으면 동적 무대를 숨기고 순서 있는 정적 상태 자료로 대체합니다.
- `scrollDistance`: 고정할 때 사용하는 추가 스크롤 거리의 `svh` 단위 값입니다. 모든 시각화에 같은 높이 공식을 적용하지 않습니다. 비고정 구간에서는 추가 여백을 만들지 않습니다.
- `stops`: 학습 진행률 0~1에서 각 실행 단계와 최종 상태가 시작되는 위치입니다. 0에서 시작해 1로 끝나는 증가 수열이며 항목 수는 `trace.length + 1`입니다.

시각화의 실제 스크롤 진행률을 `p`라고 할 때 학습 진행률은 `p / 0.9`를 0~1 범위로 제한한 값입니다. 관찰 거리의 90% 지점에서 최종 상태에 도달하고, 고정이 풀리기 전 마지막 10% 동안 완성 상태를 유지합니다. 따라서 원고의 `stops` 중 마지막 값 `1`은 실제 스크롤의 90% 지점에 해당합니다. 위로 되돌리면 같은 비율로 이전 상태를 복원합니다. 코스 도입의 마지막 15% 유지와 구별합니다.

`motion.json`은 `version: 2`와 `scenes`를 사용합니다. `scenes`에는 `visualization`으로 선택한 구간만 둡니다. 각 `steps` 항목은 `trace`의 한 단계와 대응하며 이름과 값 `nodes`를 지정합니다. 코드 예제가 있으면 강조할 코드 줄 `lines`와 그때까지의 `output`도 지정합니다. 코드 없는 관계 시각화에서는 `lines`·`output`을 생략하거나 빈 값으로 두고, 줄 번호나 실제 출력 문자열을 넣지 않습니다. `finalNodes`에는 실행 후 비교할 관계와 값을 둡니다. 반환값·출력값·이전 값을 함께 비교한다면 라벨로 시점을 구별합니다. 같은 객체는 같은 `objectId`로, 별도 복사본은 다른 `objectId`로 식별합니다.

## 본문 서식과 글자 효과

`body`의 한 단락은 문자열 또는 문구 객체의 배열입니다. 아래 예시에서 `bold`만 지정했으므로 움직임 없이 굵게 표시합니다.

```json
{
  "body": [
    "이 단락은 일반 본문입니다.",
    [
      { "text": "변수는 값에 " },
      { "text": "연결한 이름", "bold": true },
      { "text": "입니다." }
    ]
  ]
}
```

특정 문구를 움직이려면 그 문구 객체에 `effect`를 명시합니다. 코드·본문의 의미를 바꾸거나 정답·학습 이수를 판정하는 효과로 사용하지 않습니다.

```json
{
  "text": "연결한 이름",
  "bold": true,
  "effect": {
    "preset": "glow",
    "mode": "scrub",
    "purpose": "변수를 상자로 오해하지 않도록 이름과 연결이라는 표현을 강조합니다.",
    "start": "top 85%",
    "end": "top 35%",
    "duration": 1.1,
    "reentry": "repeat",
    "exit": "cancel",
    "initial": "play"
  }
}
```

| 항목 | 값과 동작 |
| --- | --- |
| `preset` | `lift`는 글자의 작은 이동, `glow`는 빛, `confetti`는 색종이, `burst`는 방사형 입자 |
| `mode` | `scrub`는 스크롤 위치에 연결. `enter`는 지정 범위에 들어올 때 짧게 재생 |
| `purpose` | 해당 문구에 효과를 붙이는 구체적인 이유 |
| `start` / `end` | 문구의 위쪽이 화면의 몇 %에 닿을 때 범위가 시작·종료되는지 지정. 시작 비율이 종료 비율보다 커야 함 |
| `duration` | 양수의 초 단위 시간. `enter`의 재생 시간으로 사용 |
| `reentry` | `enter`에서 `once`는 페이지를 읽는 동안 한 번, `repeat`는 범위에 다시 들어올 때 재생 |
| `exit` | `enter`에서 `finish`는 범위를 벗어나도 재생을 마침. `cancel`은 취소하고 정적 모습으로 복원 |
| `initial` | `enter`에서 초기 화면이 범위 안이면 `play`는 재생, `skip`은 생략 |

`enter`의 반복 진입은 문구에 머무는 동안 무한 재생한다는 뜻이 아닙니다. `scrub`는 시간·진입 이력과 무관하게 같은 위치에서 같은 표현을 표시합니다. 모든 효과는 글자를 읽을 수 있게 유지하고 줄 배치를 바꾸지 않아야 합니다. 움직임 줄이기와 효과 실패 시에는 원래 문구와 정적 서식을 남깁니다. 현재 채택한 문구와 시각화의 근거는 [콘텐츠 선택 기록](docs/content-presentation.md)에 있습니다.

## 수정 후 확인

Node.js 20 이상과 Python 3.10 이상을 준비한 뒤 실행합니다.

```sh
npm run build
npm run validate
python3 -m http.server 8765 --bind 127.0.0.1
```

`npm run validate`는 자동 검사, 생성물 일치 검사, 예제의 실제 실행·출력 대조를 순서대로 수행합니다. 코드가 없는 구간은 예제 실행 대상에서 제외합니다. 출력의 내부 공백·빈 줄은 그대로 대조하고 마지막 줄바꿈 하나만 제외합니다. `stdin`·`files`는 해당 예제의 독립된 임시 폴더에 준비합니다.

브라우저에서 변경한 레슨의 앞뒤 본문을 이어 읽으며 확인합니다.

- 코스·레슨 도입의 제목·그림·설명이 처음부터 읽히고, 스크롤 왕복·직접 주소 진입에서도 같은 위치의 배치로 복원되는지 확인합니다. 코스는 전체 고정·그림 영역 고정·정적 표시가 화면 크기에 맞게 선택되고, 관찰 구간의 마지막 15%에서 완성된 그림이 유지되어야 합니다. 작은 레슨 도입은 제목이 화면을 벗어나기 전에 연출을 마쳐야 합니다. 시간 재생을 기다리거나 고정 대상 없이 빈 공간만 지나가게 해서는 안 됩니다.
- 일반 본문·정적 예제·질문·정답·해설이 추가 입력이나 연출 재생 완료를 기다리지 않고 읽히는지 확인합니다.
- 시각화를 아래·위로 스크롤하고 같은 위치에서 코드·값·출력·상태 설명이 함께 복원되는지 대조합니다. 관찰 거리의 마지막 10%에서 최종 상태가 유지되고 이후 고정이 풀려야 합니다. 유지되어야 하는 객체·값은 그대로 남아야 합니다. 무대가 화면에 들어가지 않을 때에는 움직이며 지나가는 무대가 남지 않고 모든 상태가 정적 자료로 제공되는지 확인합니다.
- 각 글자 효과의 진입·이탈·재진입·새로 고침 동작을 작성한 규칙과 비교합니다. 글자가 가려지거나 줄 배치가 바뀌지 않아야 합니다.
- 좁은 화면·낮은 화면·확대 화면과 키보드 스크롤에서 본문과 예제를 읽을 수 있는지 확인합니다.
- 새 본문 링크는 글꼴·배치 준비 뒤 대상에 맞춰지는지, 새로고침·뒤로가기에서는 같은 주소의 읽던 위치가 복원되는지 확인합니다. 준비 중 사용자가 스크롤하거나 탐색했다면 늦은 위치 보정이 그 입력을 되돌리지 않아야 합니다.
- JavaScript 비활성화·연출 라이브러리 실패·움직임 줄이기에서는 도입 제목·완성된 상징 그림·설명과 본문, 예제, 출력, 순서대로 나열한 시각화 상태와 해설을 확인합니다. 시각화 하나의 실패가 다른 구간까지 숨기지 않아야 합니다.

정상 학습 진행은 스크롤 하나로 완결되어야 합니다. 실행 단계·재생·슬라이더·답 제출 버튼, 스크롤 잠금·강제 스냅·학습을 자동으로 진행하는 스크롤·음성·로그인·진도나 답안 수집을 추가하지 않습니다. 자동 검사 통과만으로 실제 화면을 검증했다고 기록하지 않습니다.

브라우저의 읽기 위치 복원은 학습 기록 기능과 구별합니다. `pagehide` 때 현재 URL과 가로·세로 좌표만 `history.state`에 넣고 기존 상태 속성은 보존합니다. 같은 URL의 새로고침·방문 기록 복원에만 이 좌표를 사용합니다. 서버·계정·`localStorage`에는 진도나 답안을 저장하지 않습니다. 늦은 위치 보정 전에 사용자의 스크롤·탐색 입력이 들어오면 보정을 취소합니다.

## 공개 자료의 경계

이 저장소에는 제품 코드, 직접 작성한 교육 콘텐츠, 공개 출처와 검증 방법만 담습니다. 개인 연구 기록, 연락처, 비공개 문서, 계정 정보는 포함하지 않습니다. 실제 수행한 검사와 미검증 범위를 구분해 기록합니다.
