# ScrollLand에 기여하기

ScrollLand는 스크롤만으로 설명·코드·값·실행 결과·상태 도식을 함께 갱신하며 읽는 한국어 Python 학습 자료입니다.

## 콘텐츠 수정

`curriculum.json`에 코스와 레슨 순서를, `lessons/`에 각 레슨 원본을, `motion.json`에 설명 단계와 대응하는 코드 줄·노드·출력 상태를 둡니다. 생성된 `index.html`과 `learn/`의 HTML은 직접 수정하지 않습니다.

레슨을 바꿀 때는 학습자가 해결할 문제와 목표를 먼저 확인합니다. 설명에 새 개념이 필요하면 그 뜻을 풀이하고 선행 레슨을 연결합니다. 예제 코드, 출력, 도식과 실행 단계는 서로 같은 상황을 설명해야 합니다. 자료의 출처를 적고 다른 교재의 설명이나 예제를 복사하지 않습니다.

## 확인

Node.js 20 이상과 Python 3.10 이상을 준비한 뒤 다음을 실행합니다.

```sh
npm run build
npm run validate
python3 -m http.server 8765
```

브라우저에서 `http://localhost:8765`를 열어 변경한 레슨을 읽고, 키보드와 좁은 화면에서도 사용할 수 있는지 확인합니다. JavaScript나 스크롤 연출을 사용할 수 없어도 설명, 코드, 출력, 도식과 해설을 읽을 수 있어야 합니다.

정상 학습 진행은 스크롤 하나로 완결되어야 합니다. 실행 단계·재생·슬라이더·답 제출 버튼을 추가하지 않습니다. motion.json의 상태는 해당 코드·trace·검증 출력과 대조하고 역방향에서도 같은 위치의 값·출력·설명이 함께 복원되는지 확인합니다. 스크롤 잠금·강제 스냅·자동 스크롤·음성·로그인·개인 학습 기록 수집은 추가하지 않습니다.

## 공개 자료의 경계

이 저장소에는 제품 코드, 직접 작성한 교육 콘텐츠, 공개 출처와 검증 방법만 담습니다. 개인 연구 기록, 연락처, 비공개 문서, 계정 정보는 포함하지 않습니다. 소프트웨어 검사 통과를 실제 학습 효과 검증이나 교육 내용 승인으로 표현하지 않습니다.
