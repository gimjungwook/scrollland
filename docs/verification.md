# 본문 중심 학습과 도입 구현 검증 기록

검증일: 2026-09-23. 이 기록은 `codex/reading-first`의 레슨별 설명 보강, 페이지 분리, 도입 보강을 기준본별로 구분합니다. 과거 기준본의 통과 기록을 현재 원고의 설명 충분성이나 학생 이해도의 근거로 사용하지 않습니다.

## 레슨별 설명·관찰 과정 보강

이전 기준본 `9be4003`에는 본문 시각화가 14개 레슨에만 있었고, 나머지 30개는 최종 코드·출력과 정적 자료로 설명했습니다. 작은 도입 효과가 있다는 사실과 기술 검사의 통과만으로 각 레슨의 설명이 충분하다고 본 판단을 바로잡았습니다. [레슨 설명 기준](lesson-observation.md)에 따라 모든 레슨의 혼동 지점과 관찰할 중간 과정을 다시 검토했습니다.

현재 원고는 44개 레슨의 본문 시각화 44개·중간 상태 190개, 일반 읽기 98개 구간으로 구성됩니다. 총 142개 구간 중 모든 절을 시각화하지는 않습니다. 기존 Python 예제 135개의 코드·출력·표준 입력·제공 파일은 `9be4003`과 같으며, 정의와 해석·단계 설명을 보강했습니다. 레슨별 관찰 질문과 선정 이유는 [콘텐츠 선택 기록](content-presentation.md)에 있습니다.

첫 실행의 계산과 출력, 문자열의 선택 범위, 객체 공유와 복사, 함수 호출·반환, 파일과 오류 처리의 실제 중간 상태를 구별해 검토했습니다. 반복·재사용 코스는 작성자와 별도 검토자가 Python 예제 실행 및 단계 의미를 확인했습니다. 다른 코스도 독립 검토 의견을 반영했으며, 환경 레슨은 익숙하지 않은 보조 코드 대신 환경 선택·설치를 보여주는 코드 없는 그림으로 핵심 설명을 분리했습니다.

현재 브라우저 검사의 최종 결과는 아래 표에 기록합니다. 중간 검사에서 발견한 JSON 카드 겹침, 병렬 상태의 잘못된 실행 화살표, 환경 레슨의 설명·안내 겹침은 수정 뒤 다시 검사했습니다. 검사 실패 표본을 통과 수에 포함하지 않습니다.

| 검사 | 결과와 범위 |
| --- | --- |
| 원고·생성물 | 44개 레슨·142개 구간과 190개 중간 상태의 계약 검사. 시작 1개·코스 8개·레슨 44개와 404의 생성물 및 내부 링크 검사 |
| 자동 검사 | 174개 통과. 단계별 출력·문자열 선택, 객체 진입과 고정 자리, 가장 긴 설명의 공간 확보, 설명·캡션·하단 안내 간격, 정적 대체, 기존 탐색·위치 복원 회귀 검사 포함 |
| Python 예제 | Python 3.14.6에서 기존 135개 예제 실행·예상 출력 일치. 단계 설명용 그림은 Python 실행기나 실제 메모리 측정으로 표현하지 않음 |
| 1280×900·599×921 | 각 화면에서 44개 레슨의 모든 중간 상태·완성·같은 중간으로 역스크롤·완성 상태를 확인. 각 322개 표본, 총 644개 표본에서 출력 불일치·도형 겹침·영역 이탈·설명과 안내 겹침·가로 넘침·복원 불일치 없음 |
| 360×800 | 화면에 들어오는 35개 레슨의 249개 동적 표본에서 같은 검사 통과. 나머지 9개 레슨은 전체 설명이 함께 들어가지 않아 정적 자료로 전환되며 중간 상태 46개가 모두 표시됨 |
| 정적 전환 대상 | 360×800에서 집합·중첩 자료·반복 중단·컴프리헨션·이름 범위·이터레이터와 제너레이터·람다·재귀·데코레이터. 화면에 맞지 않는 동적 무대를 잘라 표시하지 않음 |
| 정적 대체 전수 | 낮은 화면 640×450, 움직임 줄이기, JavaScript 없음, 연출 파일 실패, 인쇄 각각에서 44개 레슨의 중간 상태 190개 보존. 동적 무대 숨김·정적 자료 표시·가로 넘침 없음 확인. 연출 파일 실패 검사는 GSAP·ScrollTrigger 미정의를 함께 확인 |
| 대표 화면 | 첫 실행, 문자열 선택, JSON 완성 비교, 환경 분리, 모바일 변수 공유·재대입을 스크린샷으로 확인. 인쇄에서 숨김 속성이 정적 자료 표시를 막던 문제도 수정 |
| 검증 범위 구분 | 실제 브라우저의 코드 강조·누적 출력·그림 위치·선택·역스크롤 복원을 확인하며, 자동 검사만으로 설명 충분성을 판정하지 않음 |

최종 동적 검사 합계는 893개 표본입니다. 화면 내부의 설명 겹침을 포함하지 않았던 중간 검사의 966개 표본은 이 통과 수로 사용하지 않습니다. 환경은 macOS Chromium이며 화면 크기는 모의 설정입니다. 검사 종료 뒤 화면 크기·매체·스크립트·파일 차단 설정을 원복했습니다.

학생 대상 이해도 평가, Safari·Firefox, 실제 휴대전화와 화면 낭독기 전체 흐름 검사는 실시하지 않았습니다. 아래 과거 기준본의 문자 대비·탐색 검사는 당시 결과이며 이번 변경 전체의 새 전수 검사로 합산하지 않습니다.

## 이전 페이지 분리 기준본 `9be4003`의 검증

`f0adc1f`까지의 전체 문서는 44개 레슨을 하나의 페이지에 포함하고 있었습니다. 연속 스크롤은 레슨 안의 읽기 방식이며 전체 교육 과정을 한 페이지에 넣으라는 요구가 아니므로, [페이지 경계](page-structure.md)에 따라 시작 1개·코스 8개·레슨 44개로 나눴습니다. 아래의 이전 전체 문서 검사는 당시 구조에서 수행한 기록입니다. 새 구조의 검증 결과와 합산하지 않습니다.

| 검사 | 결과와 범위 |
| --- | --- |
| 페이지 경계와 제목 | 53개 학습 페이지의 단일 h1·중복 없는 식별자·제목 단계 확인. 시작·코스에는 레슨 본문이 없고 각 레슨에는 해당 본문 하나만 있음 |
| 원고와 예제 보존 | 이전 커밋과 `curriculum.json`·`lessons/`·`motion.json` 차이 없음. 44개 레슨의 본문 순서·135개 코드 및 출력·입력 자료·문항 해설·출처·6개 문구 효과가 정적 HTML에 들어가는지 검사 |
| 자동 검사·생성물 | 자동 검사 132개, 53개 학습 페이지와 404의 생성물·내부 파일 링크 검사 통과. Python 3.14.6에서 135개 예제 실행·출력 일치 |
| 실제 탐색 | 시작→첫 코스→변수 레슨, 예외 레슨→코스 목차, 예외→파일 읽기 레슨 이동 확인. 목적 페이지의 본문 수·제목·상단 이름 확인. 레슨 끝까지 스크롤해도 페이지 주소 유지 |
| 코스 도입 관찰 | 1280×900·360×800 각각 코스 8개와 위치 5곳, 총 80개 표본. 전체 또는 그림·설명 유지, 왕복 복원·완성 상태 유지·끝 뒤 해제, 가로 넘침 없음 |
| 개별 레슨 관찰 | 예외 레슨의 시각화를 1280×900·360×800 각각 5개 위치에서 확인. 360×800에서 작은 도입 종료 시 제목·그림 표시 확인. 360×640에서는 배치 갱신 뒤 정적 상태 자료만 표시 |
| 이전 주소 | 알려진 52개 해시의 명시적 내부 대응표와 정적 목차 식별자 검사. 실제 `index.html#lesson-exceptions` 최초 진입, 열린 목차의 `#course-objects` 변경이 각각 독립 페이지로 연결됨 |
| JavaScript 없는 탐색 | 이전 예외 레슨 해시가 시작 페이지의 해당 목차 항목을 표시. 링크를 실제 클릭해 독립 레슨 본문과 정적 상태 자료를 읽을 수 있고 가로 넘침 없음 |
| 읽기 위치 | 코스의 `#lessons` 주소는 글꼴·배치 후 목차를 표시. 독립 예외 레슨의 새로고침과 다른 페이지 방문 후 뒤로가기에서 같은 좌표·상태·그림 위치 복원 |

브라우저 확인은 macOS Chromium에서 수행했습니다. 화면 크기는 모의 설정이며 Safari·Firefox·물리 기기·학생 학습 효과 검증을 대신하지 않습니다.

## 이전 관찰 기준본 `f0adc1f`의 검증

사용자 검토에서 장면이 화면을 벗어나는 동안 연출도 진행되어 변화를 관찰하기 어렵다는 문제가 확인됐습니다. 기준본 `76bc1a4`의 검사는 위치별 상태 변화와 제목 표시를 확인했지만, 진행 내내 변화하는 그림·설명이 화면 안에 남는지는 완료 조건에 포함하지 못했습니다. 아래 기준본의 통과 기록을 이 요구의 충족 근거로 사용하지 않습니다.

코스 도입은 전체가 화면에 들어오면 전체를, 그렇지 않으면 그림·캡션·전후 미리보기만 화면에 유지합니다. 작은 레슨 도입은 계속 읽으면서 제목이 화면을 벗어나기 전에 마칩니다. 관찰 대상으로 선택한 학습 시각화 14개는 모두 고정을 요청합니다. 화면 안에 학습 무대 전체가 들어오지 않으면 동적 무대를 숨기고 순서 있는 정적 자료만 표시합니다. 일반 읽기 127개 구간은 고정하지 않습니다.

코스 도입은 관찰 거리의 85%, 학습 시각화는 90%에서 완성 상태에 도달합니다. 나머지 거리에서 결과를 확인한 뒤 자연스럽게 다음 본문으로 이동합니다. 스크롤 잠금·강제 스냅·시간 대기·자동 학습 이동은 없습니다.

| 실제 브라우저 검사 | 확인 결과 |
| --- | --- |
| 1280×900 | 코스 8개 전체 도입과 시각화 14개가 관찰 중 화면 안에 머무름. 각각 5개 위치에서 110개 표본 검사 |
| 360×800 | 코스 8개의 그림·캡션·전후 미리보기와 시각화 14개가 화면 안에 머무름. 110개 표본 검사 |
| 640×450·360×640 | 각 크기에서 코스 그림 8개·40개 표본 확인. 학습 시각화 14개는 동적 무대가 숨겨지고 모든 상태를 순서대로 읽음 |
| 왕복·완성 상태 | 위 총 300개 표본에서 가로 넘침·관찰 영역의 화면 이탈 없음. 같은 중간 위치의 보이는 값·그림·연결 복원, 마지막 구간의 완성 상태 유지 확인. 코스는 종료 뒤 120px 더 내려가면 고정이 해제됨 |
| 짧은 레슨 도입 | 360×800·640×450 각각 44개 레슨의 연출 종료 위치를 확인. 제목과 작은 그림이 모두 화면 안에 남음 |
| 움직임 줄이기·JavaScript 없음·연출 파일 실패·인쇄 | 제목 52개와 순서 있는 상태 자료 14개 유지. 고정 영역 0개, 관찰용 추가 높이 해제. 인쇄의 2px 테두리는 내용 구분선이며 스크롤 거리가 아님 |
| 직접 주소·읽기 위치 복원 | 최종 배치 뒤 함수 레슨의 직접 주소가 화면 안에 정렬됨. 객체 코스의 관찰 중간 위치에서 새로고침·다른 레슨 방문 후 뒤로가기 모두 같은 좌표와 진행률 0.5로 복원됨. 복원 수정 후 1280×900의 110개 관찰 표본도 다시 통과 |
| 자동 검사·생성물·예제 | 위치 복원 검사 15개를 포함한 자동 검사 124개 통과. 8개 코스·44개 레슨의 생성물·내부 링크 정상. Python 3.14.6에서 독립 예제 135개 실행·출력 일치 |

검사는 macOS의 Chromium에서 화면 크기를 모의 설정해 수행했습니다. 물리 기기의 터치감이나 학생의 이해도 검증과 구별합니다.

관찰 구간의 추가 높이 때문에 브라우저가 초기 정적 배치에 맞춰 읽기 위치를 복원하면 뒤쪽 레슨이 어긋나는 문제도 고쳤습니다. 현재 방문 기록의 주소·좌표만 `history.state`에 남기고 글꼴·최종 배치가 준비된 뒤 복원합니다. 먼저 사용자가 스크롤하거나 다른 주소를 선택하면 늦은 보정을 취소합니다. 진도·답안 저장이나 서버 전송은 하지 않습니다.

## 이전 도입 기준본 `76bc1a4`에서 수행한 검사

8개 코스에 서로 다른 상징 그림과 주제 제목을 넣고, 44개 레슨에 해당 그림의 작은 변형·구체적인 질문·선행 학습 연결·결과 미리보기를 작성했습니다. 첫 코스는 서비스 도입을 겸하며, 개별 레슨 주소에는 짧은 레슨 도입만 표시합니다. 기존 교육 원본의 `problem`은 보존하고 화면에서는 `intro.hook`과 같은 질문을 중복하지 않습니다. 도입 제목과 설명은 처음부터 읽히며, 스크롤은 그림 조각과 제목의 배치·선의 표시만 바꿉니다.

환경: macOS arm64, Node.js 22.23.2, Python 3.14.6, Codex 인앱 브라우저의 Chromium. 화면 크기는 모의 설정입니다.

| 검사 | 결과와 범위 |
| --- | --- |
| 도입 자료·교육 원본 | 8개 코스·44개 레슨의 필수 도입 자료 확인. `d64df26`과 비교해 `intro`를 제외한 교육 원본이 동일함을 확인. 선택한 미리보기의 근거 예제 44개도 각각 실행·출력 대조 |
| 자동 검사 | 103개 통과. 도입 계약·정적 생성·같은 위치의 배치 복원·기존 스타일 복원·개별 실패·최초 진입·매체 설정 변경·최소 양의 스크롤 거리 포함 |
| 전체 예제·생성물 | 135개 Python 예제 출력 일치. 전체 문서와 개별 레슨 44개·404의 생성물, 필수 자산·내부 파일 링크 검사 통과 |
| 실제 스크롤 | 1280×900, 360×800, 640×450에서 도입 52개씩 진행률 0.25→0.62→1→0.62로 이동. 총 624개 표본에서 제목이 표시되고 가로 넘침이 없었으며 같은 중간 위치의 스타일이 모두 복원됨 |
| 진행률 연결 | 52개 도입 각각에서 실제 스크롤과 표시 진행률을 대조. 요청 진행률과 차이가 0.01 이내이며 처음과 끝의 배치가 달라짐을 확인 |
| 정적 대체 | 움직임 줄이기, JavaScript 비활성화, GSAP·ScrollTrigger 파일 차단, 인쇄의 네 조건에서 도입 52개의 제목·설명·완성된 그림 유지. 동적 변형 0개, 숨겨진 도입 설명 0개. 검사 후 설정·차단 해제 |
| 위치 안내 | 코스 세 곳·레슨 두 곳·첫 코스를 빠르게 왕복해 상단 이름이 현재 코스·레슨과 일치하는지 확인 |
| 개별 레슨 | 함수 레슨의 직접 주소에서 코스 도입 0개·레슨 도입 1개 확인. 코스 제목 링크로 전체 문서의 해당 코스 도입에 도착 |
| 제목 위계 | 전체 문서의 서비스→코스→레슨→구간·문항·요약→입력 자료를 h1→h2→h3→h4→h5로 구별. 개별 레슨은 h1→h2→h3. 생성 페이지 45개에 h1이 하나씩 있고 중간 단계 생략이 없음 |
| 문자 대비 | 도입의 실제 텍스트 요소 725개의 계산된 전경·합성 배경색을 대조. 최솟값 5.25:1, 4.5:1 미만 0개. SVG 장식·선과 학습 효과 판정은 포함하지 않음 |
| 대표 화면 | 첫 코스, 묶음·반복·도구·저장 코스의 서로 다른 그림과 제목 배치, 데스크톱·모바일 함수 레슨을 스크린샷으로 확인 |

처음 방문할 때 첫 코스의 연출이 이미 끝나는 진행 범위를 고쳤습니다. 모바일에서는 도입 질문을 제목 옆의 좁은 칸에서 전체 폭으로 옮겼습니다. 묶음 그림의 위치 번호는 설명과 맞게 0·1·2로 표시합니다. 코스 사이를 빠르게 이동할 때 이전 레슨명이 상단에 남던 문제도 고쳤습니다. 제목·그림을 지나가기 위한 고정·추가 스크롤 높이·시간 대기는 없습니다.

아래 본문·시각화 상세 결과는 도입 보강 전 기준본 `d64df26`에서 수행한 검사입니다. 이번 도입 검사와 합산해 동일 커밋의 새 전수 검사로 주장하지 않습니다. 현재 교육 원본 보존과 전체 자동 검사는 위에서 별도로 확인했습니다. 제목 위계 교정 후에는 360×800의 52개 도입·208개 표본을 다시 확인해 동일하게 통과했습니다.

## 구현 범위

8개 코스·44개 레슨과 독립 실행 예제 135개를 보존했습니다. 원고는 읽기 127개 구간과 시각화 14개 구간으로 구성했습니다. 읽기 중 6개는 글만 있으며, 정적 비교 자료 39개와 명시한 문구 효과 6개를 사용합니다. 이 개수는 원고별 선택 결과이며 제작 할당량이 아닙니다. [구간별 선택 근거](content-presentation.md)와 [교육 범위](curriculum-coverage.md)를 함께 확인할 수 있습니다.

한 레슨의 본문·정적 예제·확인 문항은 일반 문서 흐름에 남습니다. 선택한 시각화에만 상태 계산을 연결하며, 문구 효과는 별도 자료와 실행 경로를 사용합니다. 레슨 안의 내용은 스크롤로 읽고 코스·레슨 사이는 제목을 명시한 링크로 이동합니다. JavaScript가 없어도 해당 페이지의 학습 자료가 HTML에 들어 있습니다.

## 본문 중심 기준본에서 확인한 결과

환경: macOS arm64, Node.js 22.23.2, Python 3.14.6, Codex 인앱 브라우저의 Chromium. 브라우저 크기는 모의 설정이며 실제 휴대전화 검사를 뜻하지 않습니다.

| 검사 | 결과와 범위 |
| --- | --- |
| 자동 검사 | 78개 통과. 읽기 구간·효과·선택 시각화 계약, 코드 없는 관계 시각화, 실제 제어기의 DOM 대역 검사, 국소 실패, 초기 주소·왕복 복원, 코드 표시 위치, 문구 재생·입자 경계 규칙 포함 |
| 생성물 | 전체 여정·개별 레슨 44개·404의 원본 일치, 필수 자산, 내부 파일 링크 검사 통과 |
| 오류 주소 | 임시 검사 서버에서 없는 주소가 HTTP 404와 제품 안내·목차 링크를 반환하는지 확인. 운영 호스팅의 설정 검증과 구별 |
| Python 예제 | 135개를 독립 임시 폴더에서 실행해 예상 출력과 대조. 135개 모두 일치. [세부 기록](content-verification.md) |
| 원고 보존 | 이전 공개 코드와 대조해 기존 본문 문장 순서·코드·출력·제공 입력·파일·목표·선행 개념·문항·출처가 보존됨을 확인 |
| 전체 여정 | 실제 DOM에서 레슨 44개, 읽기 127개, 시각화 14개, 정상 초기화한 문구 효과 6개 확인 |
| 본문·문항 | 본문 141곳은 연속해서 남음. 질문·세 선택지·정답과 근거 132개가 정상 문서 흐름에 존재. 장문 교체·답 공개 대기 없음 |
| 시각화 배치 | 1280×900, 768×900, 360×800, 1280×400, 640×450, 1280×720, 360×640의 일곱 크기에서 14개 시각화 각각 진행률 0.15·0.5·0.9·1을 검사. 총 392개 표본에서 노드 겹침·그림 영역 이탈·무대 내용 넘침 없음. 최종 입자 수정 후 360×800에서 시각화 56개 표본과 두 입자 효과를 재검사해 페이지 가로 넘침이 없음을 확인 |
| 왕복 복원 | 14개 시각화에서 실제 스크롤을 처음→중간→끝→같은 중간으로 이동. 코드 강조·값·연결·좌표·출력·상태 문장과 본문이 같은 값으로 복원됨 |
| 대표 화면 | 변수 본문·정적 코드·이름 공유와 재대입, 모바일 중첩 자료와 파일 읽기·닫힘, 짧은 도입과 종합 예제의 문구 효과를 스크린샷으로 확인 |
| 글자 효과 | 글자 이동·빛 강조·폭죽·색종이의 실제 스타일/입자 변화 확인. 색종이 재생 중·종료 후 문구와 부모 문단 높이 97px가 동일했고 종료 후 입자는 0개 표시. 화면 전체 효과나 읽기 대기 없음 |
| 낮은 화면 | 고정을 해제하고 순서 있는 정적 상태 자료도 아래에 제공. 화면에 들어오지 않는 무대의 앞 상태를 놓쳐도 읽을 수 있음 |
| 키보드 | 실제 Home으로 처음, Page Down으로 아래, End로 끝까지 이동. 학습 진행용 폼·재생·답 제출 조작 없음. 링크·정적 코드 영역에 초점 표시 제공 |
| 문자 대비 | 실제 렌더링한 텍스트 말단 요소 4,692개의 계산된 전경·합성 배경색을 대조. 최소 4.88:1, 4.5:1 미만 0개. 선·장식·움직이는 입자의 대비 판정과 구별 |
| 움직임 줄이기 | 열린 페이지에서 설정 변경 후 시각화 무대 0개, 정적 상태 자료 14개, 본문 141개 유지. 입자 요소 0개. 설정 복원 후 정상 연출로 복귀 |
| JavaScript 없음 | 실행을 비활성화하고 페이지를 다시 읽어 정적 예제 135개·상태 자료 14개·본문 141개·문항 근거 132개 확인 |
| 연출 파일 실패 | GSAP·ScrollTrigger 파일만 차단하고 다시 읽어 같은 정적 자료와 모든 본문·근거 유지 확인. 검사 후 차단 해제 |

자동 검사는 교육적 타당성이나 학생의 이해도를 증명하지 않습니다. DOM 대역 검사와 실제 브라우저 검사를 구분했습니다. 상태 자료의 전수 의미 검증은 자동 검사이며, 위 배치 검사는 명시한 표본 위치의 실측입니다.

## 검수 중 고친 문제

- 읽기 구간에 예제·도식·실행 단계 개수를 강제하던 계약, 모든 구간 고정, 본문 교체, 긴 정답 공개 구간을 제거했습니다.
- 긴 코드 전체 높이가 무대를 늘리던 문제를 고쳤습니다. 코드 표시 위치는 해당 실행 줄을 따라가며 별도 내부 조작을 요구하지 않습니다.
- 동일한 이름·객체의 자리를 시각화 전체에서 유지하고, 새 상태 문장 아래에 이전 값·연결이 남던 전환 불일치를 제거했습니다.
- 정적 대체본의 중복 코드 줄이 강조·위치 계산에 섞이지 않도록 범위를 제한했습니다.
- 긴 리스트 값의 과도한 확대와 낮은 화면의 도식 영역 이탈, 정적 코드 줄 번호의 부족한 대비를 보정했습니다.
- 초기 주소 이동을 마친 뒤 문구 효과를 준비해 최초 표시 시 재생하지 않는 설정을 지킵니다. 단어 효과 실패는 원문을 남기고 구현 오류만 기록합니다.
- 고정하지 않는 시각화의 진행 거리를 아래 정적 설명의 높이와 분리했습니다.
- 문구 효과의 입자를 읽기 영역과 화면 안에 제한하고, 종료·취소된 투명 입자의 변형 좌표를 제거했습니다. 360×800 새 방문에서 폭죽·색종이 각각 10개 입자가 나타나는 동안 75프레임씩 검사해 가로 넘침 0px를 확인했습니다.

## 미실시 항목과 공개 상태

640×450 검사는 1280×900 화면을 200% 확대했을 때와 같은 CSS 배치 폭·높이를 점검한 것입니다. 브라우저 메뉴의 실제 200% 확대와 화면 낭독기 전체 흐름, Safari·Firefox, 물리 휴대전화, 저성능 기기의 성능은 별도 검사가 필요합니다. 이번 결과로 모든 브라우저의 접근성 인증을 주장하지 않습니다.

GitHub 소스 공개와 운영 웹사이트 배포는 별개입니다. 이 변경은 공개 브랜치와 검토 요청으로 전달하며 main 병합·운영 배포·교수 승인·학생 학습 효과 검증을 완료로 기록하지 않습니다. GitHub Actions는 Python 3.10·3.14에서 같은 자동 검사를 실행합니다. 특정 공개 커밋의 통과 여부는 해당 커밋의 Actions 결과로 확인해야 합니다.

## 재현

`npm run build` 후 `npm run validate`를 실행합니다. 스타일·실행 코드를 바꾸면 대표 화면과 해당 접근성 경로를 실제 브라우저에서도 다시 확인합니다. 개발 서버는 `python3 -m http.server 8765 --bind 127.0.0.1`로 시작합니다. 이 서버의 기본 오류 화면은 제품의 404와 다르므로, 사용자 정의 404를 제공하는 검사 서버나 실제 정적 호스팅에서 오류 경로를 별도로 확인합니다.
